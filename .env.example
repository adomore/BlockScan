# Copy to `.env` and fill in your values.

# Ethereum JSON-RPC endpoint (Infura / Alchemy / your own node, etc.)
ETH_RPC_URL=https://eth-mainnet.g.alchemy.com/v2/YOUR_KEY

# Etherscan API key (https://etherscan.io/myapikey). V2 API works across chains.
ETHERSCAN_API_KEY=YOUR_ETHERSCAN_KEY
